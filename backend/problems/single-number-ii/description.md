Given an integer array nums where every element appears exactly three times except for one, which appears exactly once. Find the single element and return it. Your solution must have O(1) extra space complexity and O(n) time complexity.