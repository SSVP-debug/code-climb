Implement a Trie class that supports: insert(word) — inserts word into the trie; countWordsEqualTo(word) — returns the number of instances of word that have been inserted; countWordsStartingWith(prefix) — returns the number of previously inserted strings that have prefix as a prefix; and erase(word) — removes one instance of word from the trie (word is guaranteed to be present when erase is called).